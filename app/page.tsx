"use client";
import {useState} from 'react';
import {Activity, ArrowUpRight, ArrowRight, Globe2, Search, Download, Check, AlertTriangle, ChevronDown, Zap, ScanLine, Code2, Smartphone, CircleHelp, Loader2, RotateCw} from 'lucide-react';
import {Tabs,TabsList,TabsTrigger,TabsContent} from '@/components/ui/tabs';
import {Select,SelectTrigger,SelectValue,SelectContent,SelectItem} from '@/components/ui/select';
import {Progress} from '@/components/ui/progress';
import {Skeleton} from '@/components/ui/skeleton';
type CheckItem={name:string;pass:boolean;detail:string;fix:string;category:string};
type Report={url:string;title:string;description:string;score:number;checks:CheckItem[];ttfb:number;load:number;bytes:number;images:number;scripts:number;styles:number;date:string};
const example:Report={url:'https://example.com/',title:'Example website — A better place to start',description:'An illustrative report showing how Sitepulse turns website checks into a clear action plan.',score:80,ttfb:342,load:486,bytes:68420,images:18,scripts:7,styles:3,date:'',checks:[{name:'Page title',pass:true,detail:'A unique page title is present.',category:'Content',fix:''},{name:'Meta description',pass:true,detail:'A descriptive page summary is present.',category:'Content',fix:''},{name:'Main heading',pass:true,detail:'1 H1 heading found.',category:'Content',fix:''},{name:'Image alternative text',pass:false,detail:'4 of 18 images are missing alt attributes.',category:'Accessibility',fix:'Add descriptive alt text to informative images. Use an empty alt attribute for decorative images.'},{name:'Canonical URL',pass:false,detail:'No canonical link found.',category:'Technical',fix:'Add a canonical link in the page head to identify the preferred URL.'},{name:'Mobile viewport',pass:true,detail:'Viewport metadata present.',category:'Technical',fix:''},{name:'Search indexing',pass:true,detail:'No noindex directive found.',category:'Technical',fix:''},{name:'Secure connection',pass:true,detail:'Page served over HTTPS.',category:'Technical',fix:''},{name:'Social sharing metadata',pass:true,detail:'Open Graph title and description present.',category:'Content',fix:''},{name:'Document language',pass:true,detail:'English language declared.',category:'Accessibility',fix:''}]};
export default function Home(){
const [url,setUrl]=useState('');const [report,setReport]=useState<Report>(example);const [demo,setDemo]=useState(true);const [loading,setLoading]=useState(false);const [error,setError]=useState('');const [tab,setTab]=useState('overview');const [filter,setFilter]=useState('all');
const failed=report.checks.filter(c=>!c.pass);const passed=report.checks.length-failed.length;
async function analyze(e:React.FormEvent){e.preventDefault();setError('');setLoading(true);try{const r=await fetch('/api/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url})});const data=await r.json() as Report & {error?:string};if(!r.ok)throw new Error(data.error);setReport(data);setDemo(false);setTab('overview');}catch(e){setError(e instanceof Error?e.message:'Analysis failed. Try again.');}finally{setLoading(false);}}
function download(){const blob=new Blob([JSON.stringify({...report,illustrative:demo},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`sitepulse-${new URL(report.url).hostname}.json`;a.click();URL.revokeObjectURL(a.href);}
function checklist(items:CheckItem[]){return <div className="checklist">{items.map(c=><details key={c.name}><summary><span className={'check-icon '+(c.pass?'good':'warn')}>{c.pass?<Check size={16}/>:<AlertTriangle size={16}/>}</span><span className="check-name">{c.name}<small>{c.category}</small></span><span className={'badge '+(c.pass?'good':'warn')}>{c.pass?'Passed':'Needs attention'}</span><ChevronDown size={16}/></summary><div className="check-detail"><p>{c.detail}</p>{!c.pass&&<p><strong>Recommended fix:</strong> {c.fix}</p>}</div></details>)}</div>}
return <div className="app"><header><a className="brand" href="/" aria-label="Sitepulse home"><span className="brand-icon"><Activity size={23}/></span>sitepulse<span className="brand-dot">.</span></a><span className="header-label">Website intelligence</span><a className="header-help" href="#methodology"><CircleHelp size={17}/> How it works</a><div className="avatar">SP</div></header><main><div className="heading-row"><div><div className="eyebrow">YOUR WEBSITE, UNDERSTOOD</div><h1>A clearer picture of your website.</h1><p>Find what’s working. Fix what’s holding you back.</p></div><span className="workspace-tag"><ScanLine size={16}/> Website audit</span></div><form onSubmit={analyze} className="audit-form"><Globe2 size={21}/><label className="sr-only" htmlFor="website">Website URL</label><input id="website" value={url} onChange={e=>setUrl(e.target.value)} placeholder="Enter your website URL" required disabled={loading}/><span className="public-label">Public websites</span><button className="primary" disabled={loading}>{loading?<Loader2 className="spin" size={17}/>:<Zap size={17}/>} {loading?'Analyzing…':'Analyze website'}{!loading&&<ArrowRight size={16}/>}</button></form>{error&&<div role="alert" className="error"><AlertTriangle size={18}/>{error}</div>}
<section className="report" aria-busy={loading}><div className="report-top"><div className="domain"><span className="domain-icon"><Globe2 size={23}/></span><div><h2>{new URL(report.url).hostname} <ArrowUpRight size={16}/></h2><span>{demo?'Explore an illustrative report before running your audit.':`Analyzed ${new Date(report.date).toLocaleString()} · Single page audit`}</span></div></div><div className="report-actions">{demo&&<span className="demo-badge">Example report</span>}<button className="secondary" onClick={download} disabled={loading}><Download size={16}/> Export report</button></div></div>
{loading?<div className="loading-state" role="status"><Loader2 size={28} className="spin"/><h3>Taking a closer look…</h3><p>Fetching page content and checking SEO signals.</p><div className="skeleton-grid">{[1,2,3,4].map(n=><Skeleton key={n} className="h-36 w-full rounded-xl"/>)}</div></div>:<><Tabs value={tab} onValueChange={setTab}><TabsList variant="line" className="report-tabs"><TabsTrigger value="overview"><Activity/> Overview</TabsTrigger><TabsTrigger value="seo"><Search/> SEO analysis <span className="tab-count">{report.checks.length}</span></TabsTrigger><TabsTrigger value="performance"><Zap/> Performance</TabsTrigger></TabsList><TabsContent value="overview"><div className="score-grid"><div className="score-card lead"><div className="card-label">SEO health <Search size={16}/></div><div className="score-body"><div><strong>{report.score}<em>/ 100</em></strong><span className="score-caption">{report.score>=90?'Looking healthy':report.score>=60?'Room to improve':'Needs attention'}</span></div><div className="ring" style={{'--score':`${report.score}%`} as React.CSSProperties}><Check size={22}/></div></div><p>{passed} of {report.checks.length} checks passed</p></div><Metric title="Server response" value={`${(report.ttfb/1000).toFixed(2)}`} unit="s" icon={<Zap size={16}/>} caption="Time to response headers"/><Metric title="HTML page weight" value={`${(report.bytes/1024).toFixed(1)}`} unit="KB" icon={<Code2 size={16}/>} caption="Uncompressed page HTML"/><Metric title="Opportunities" value={String(failed.length).padStart(2,'0')} icon={<ScanLine size={16}/>} caption="Actionable SEO improvements"/></div><div className="overview-grid"><section className="panel"><div className="panel-heading"><div><h3>Make your next move count</h3><p>Your highest-priority opportunities, in one place.</p></div><span className="count-label">{failed.length} to improve</span></div>{failed.length?checklist(failed):<div className="success-empty"><Check/> All checks passed. Your page has the essentials covered.</div>}<button className="text-button" onClick={()=>setTab('seo')}>View all SEO checks <ArrowRight size={16}/></button></section><section className="panel health-panel"><div className="panel-heading"><h3>Audit breakdown</h3><span className="muted">{report.checks.length} checks</span></div><div className="health-number">{passed}<span>checks passed</span><span className="healthy-tag">{report.score}%</span></div><div className="segments">{report.checks.map((c,i)=><span key={i} className={c.pass?'pass-segment':'warning-segment'}/>)}</div><div className="legend"><span><i className="green-dot"/>Passed <b>{passed}</b></span><span><i className="orange-dot"/>Needs attention <b>{failed.length}</b></span></div><div className="health-note"><span className="note-icon"><Activity size={20}/></span><p>Small improvements add up.<br/><strong>Start with your page essentials.</strong></p></div></section></div><section className="performance-strip"><div className="strip-icon"><Zap size={22}/></div><div><h3>Go beyond the score</h3><p>Explore response timing, page weight, and resource counts.</p></div><button className="secondary" onClick={()=>setTab('performance')}>Explore performance <ArrowUpRight size={16}/></button></section></TabsContent><TabsContent value="seo"><section className="panel seo-panel"><div className="panel-heading"><div><h3>SEO checks</h3><p>Inspect the signals found in your page’s HTML.</p></div><label className="filter">Show <Select value={filter} onValueChange={setFilter}><SelectTrigger aria-label="Filter SEO checks"><SelectValue/></SelectTrigger><SelectContent><SelectItem value="all">All checks</SelectItem><SelectItem value="issues">Needs attention</SelectItem><SelectItem value="passed">Passed</SelectItem></SelectContent></Select></label></div>{checklist(report.checks.filter(c=>filter==='all'||(filter==='issues'?!c.pass:c.pass)))}{filter==='issues'&&!failed.length&&<p className="success-empty">No issues found in these checks.</p>}</section><section className="panel search-preview"><h3>Search preview</h3><p className="preview-url">{report.url}</p><h4>{report.title||'No page title found'}</h4><p>{report.description||'No meta description found.'}</p><small>Illustrative preview. Search engines may display different text.</small></section></TabsContent><TabsContent value="performance"><div className="score-grid"><Metric title="Response headers" value={(report.ttfb/1000).toFixed(2)} unit="s" icon={<Zap size={16}/>} caption="Measured from the audit server"/><Metric title="HTML download" value={(report.load/1000).toFixed(2)} unit="s" icon={<Download size={16}/>} caption="Total fetch time, including headers"/><Metric title="HTML size" value={(report.bytes/1024).toFixed(1)} unit="KB" icon={<Code2 size={16}/>} caption="Decoded response body"/><Metric title="Images" value={String(report.images)} icon={<Globe2 size={16}/>} caption="Image elements in page HTML"/></div><section className="panel resources"><h3>Page resource inventory</h3><p>Referenced resources found in the original HTML.</p>{[['Images',report.images],['External scripts',report.scripts],['Stylesheets',report.styles]].map(([label,num])=><div className="resource-row" key={label}><span>{label}</span><Progress value={Math.min(100,Number(num)/Math.max(1,report.images,report.scripts,report.styles)*100)}/><b>{num}</b></div>)}<div className="measurement-note"><Smartphone size={22}/><p><strong>A server-side snapshot</strong><br/>These timings describe a single HTML fetch, not a browser page load. JavaScript is not executed and resources are not downloaded. Core Web Vitals and Lighthouse scores require a browser-based test.</p></div></section></TabsContent></Tabs></>}
</section><footer id="methodology"><span><Activity size={16}/> Built for a healthier web.</span><p>Single-page HTML checks · Equal-weight SEO score · No browser rendering{demo?' · Sample data':''}</p></footer></main></div>
}
function Metric({title,value,unit,icon,caption}:{title:string;value:string;unit?:string;icon:React.ReactNode;caption:string}){return <div className="score-card"><div className="card-label">{title}{icon}</div><div className="score-body"><strong>{value}<em>{unit}</em></strong></div><p>{caption}</p></div>}


